# Laravel + PostgreSQL

AppThrust application template. The container listens on port 8080.

## Local Docker check

```bash
docker build -t template-laravel:local .
docker run --rm -p 8080:8080 template-laravel:local
```
